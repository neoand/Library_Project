<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="module_category_library" model="ir.module.category">
            <field name="name">Library</field>
            <field name="description">Helps you manage a library</field>
            <field name="sequence">10</field>
        </record>

        <record id="group_library_user" model="res.groups">
            <field name="name">User</field>
            <field name="category_id" ref="module_category_library"/>
            <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
        </record>

        <record id="group_library_manager" model="res.groups">
            <field name="name">Manager</field>
            <field name="category_id" ref="module_category_library"/>
            <field name="implied_ids" eval="[(4, ref('group_library_user'))]"/>
            <field name="users" eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"/>
        </record>

    </data>

    <data noupdate="0">
        <!-- As regras de acesso estão agora em ir.model.access.csv -->
        <!-- As regras de registro (record rules) podem ser adicionadas aqui se necessário -->

        <!-- Exemplo de Record Rule (descomente se precisar):
        <record id="book_loan_user_rule" model="ir.rule">
            <field name="name">Library Loan: User can only see their own loans</field>
            <field name="model_id" ref="model_library_book_loan"/>
            <field name="groups" eval="[(4, ref('group_library_user'))]"/>
            <field name="domain_force">[('partner_id', '=', user.partner_id.id)]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>
        -->
    </data>
</odoo>
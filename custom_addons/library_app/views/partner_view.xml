<?xml version="1.0" encoding="UTF-8"?>
<odoo>
  <record id="view_res_partner_author_form" model="ir.ui.view">
    <field name="name">res.partner.author.form.inherit</field>
    <field name="model">res.partner</field>
    <field name="inherit_id" ref="base.view_partner_form"/>
    <field name="arch" type="xml">

      <!-- insere campos antes do notebook padrão -->
      <xpath expr="//sheet/notebook" position="before">
        <group string="Author Info">
          <field name="is_author"/>
          <field name="birth_date"/>
          <field name="death_date"/>
          <field name="birth_place"/>
        </group>
        <group string="Author Metrics">
          <field name="book_count" readonly="1"/>
          <field name="first_publication" readonly="1"/>
          <field name="last_publication" readonly="1"/>
        </group>
      </xpath>

      <!-- acrescenta páginas ao notebook -->
      <xpath expr="//notebook" position="inside">
        <page string="Biography">
          <field name="biography"/>
        </page>
        <page string="Awards &amp; Website">
          <group>
            <field name="awards"/>
            <field name="website"/>
          </group>
        </page>
      </xpath>

      <!-- adiciona botão sem usar attrs -->
      <xpath expr="//sheet" position="inside">
        <button name="action_open_author_books"
                type="object"
                string="View Books"
                class="btn-primary"/>
      </xpath>

    </field>
  </record>
</odoo>